# CS348 Semester Project - SAM HREN

```bash
docker compose down -v && docker compose up -d
```

## AI Usage

AI was used only for styling in the frontend. I reviewed and modified the generated code before use, had daisyUI implemented via claude code.
